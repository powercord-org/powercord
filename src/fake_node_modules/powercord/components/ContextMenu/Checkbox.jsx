const { React } = require('powercord/webpack');

module.exports = class CheckboxItem extends React.PureComponent {
  constructor (props) {
    super(props);

    this.state = {
      checked: props.defaultState
    };
  }

  onClick () {
    const checked = !this.state.checked;

    this.props.onToggle(checked);
    this.setState({ checked });
  }

  render () {
    return (
      <div
        className='item-1Yvehc itemToggle-S7XGOQ'
        role='button'
        onClick={() => this.onClick()}
      >
        <div className='label-JWQiNe'>
          {this.props.name}
        </div>

        <div className='checkbox' role='button'>
          <div className='checkbox-inner'>
            <input type='checkbox' checked={this.state.checked} />
            <span />
          </div>
        </div>
      </div>
    )
  }
}