const { React } = require('powercord/webpack');

module.exports = class Toast extends React.Component {
  constructor (props) {
    super(props);

    this.state = {
      header: props.header,
      buttons: props.buttons,
      content: props.content
    };
  }

  onBtnClick (onClick) {
    return onClick(state => Promise.resolve(this.setState(state)));
  }

  render () {
    const { props, state } = this;

    return (
      <div
        className={`powercord-toast${state.leaving ? ' leaving' : ''}`}
        style={props.style}
      >
        {state.header && (
          <div class='header'>
            {state.header}
          </div>
        )}

        <div className={state.fade ? `fade${state.fade}` : ''}>
          {state.content && (
            <div className='content'>
              {state.content}
            </div>
          )}
      
          <div class='btns'>
            {state.buttons.map(btn => (
              <div class='btn' onClick={this.onBtnClick.bind(this, btn.onClick)}>
                {btn.text}
              </div>
            ))}
          </div>
        </div>
      </div>
    );
  }
}