const { getModuleByDisplayName, React } = require('powercord/webpack'); // eslint-disable-line no-unused-vars
const Input = getModuleByDisplayName('TextInput');

const FormItem = require('./FormItem.jsx');

module.exports = () => {
  const { children: title, note, required } = this.props;
  delete this.props.children;

  return (
    <FormItem title={title} note={note} required={required}>
      <Input {...this.props} />
    </FormItem>
  );
};
