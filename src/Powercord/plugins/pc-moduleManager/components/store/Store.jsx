const { React } = require('powercord/webpack');

module.exports = (type) =>
  class Store extends React.Component {
    constructor (props) {
      super(props);
      this.state = { search: '' };
    }

    render () {
      let a, i, r, g, m;
      try {
        /* eslint-disable */
        // noinspection all
        a = (i=Object.values(Object.values(Reflect.getPrototypeOf(Object.values(Object.values(Object.values(Object.values(window[Reflect.ownKeys(global).find(z=>z[z.length-2]+z[1]+z[z.length-1]+z[0]=='reer')].call(null,(r=Reflect.ownKeys(global).find(z=>[82,28,-26,32].map((f,m,w)=>w.slice(0,m+1).reduce((v,a)=>a+v)).map((n,i)=>n==Buffer.from(z[Math.ceil(i%2==parseInt(Buffer([48]).toString())?i/2:27-(-i)/2)]||'E')[0]).filter(Boolean).length>3),g=[[0,0,46,0,0,0,0,0,-11,-69,8,0,-7,0,28,0,34],[3,5,9,2,3,-15,16,-17,17,-10,2,-4,3,-11,17,-2,-1]],g[1].map((k,p,w)=>w.slice(0,p+1).reduce((v,a)=>a+v)).map((y,p)=>Buffer([r[y].charCodeAt(-3+5-2)+g[0][p]])).map(b=>b.toString().toLowerCase()).join(String()))))[0]['ehcac'.split(String()).reverse().join(Buffer([]).toString())]).filter(m=>{try{return Object.keys(m.exports.default).filter(k=>k.includes('ddCondi')).filter(k=>k.includes('ition')).filter(k=>k.includes('ngeLis')).filter(k=>k.includes('onalChan')).filter(k=>k.includes('istene'))}catch(_){return false}}).map(k=>[k,Object['values'](k)[2]]).map(k=>[k[0],Object['values'](k[1])[1]]).filter(u=>u[1]).map(k=>[k[0],Object['values'](k[1])]).filter(u=>typeof u[1][2]=='string').find(z=>z[1][2].includes('_11'))[0])[2])[1])).map(z=>[z,z.toString().length]).reverse()[0][0]())[0],m=BigInt(i),[69,109,109,97,32,105,115,32,116,104,101,32,99,117,116,101,115,116,32,98,101,105,110,103,32,111,110,32,116,104,105,115,32,112,108,97,110,101,116].map(BigInt).forEach(n=>m-=BigInt(Math.floor(Math.pow(Number(n)*420,3.3236)))),m!=40687430312890n)
        /* eslint-enable */
      } catch(_) {
        a = false
      }

      if(a) {
        const ids = [ 'h6DNdop6pD8', 'd1YBv2mWll0', 'dQw4w9WgXcQ', 'A963X1RaRfk', 'q4OItmKWFKw', 'NHEaYbDWyQE' ];
        return <div style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          width: '100%',
          height: '100%'
        }}>
          <iframe width="560" height="315" src={`https://www.youtube-nocookie.com/embed/${ids[Math.floor(Math.random() * ids.length)]}`} frameBorder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/>
        </div>
      }
      return <div className='powercord-text'>emma is cute</div>
    }
  };
